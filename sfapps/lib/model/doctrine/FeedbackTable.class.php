<?php

/**
 * FeedbackTable
 * 
 * This class has been auto-generated by the Doctrine ORM Framework
 */
class FeedbackTable extends Doctrine_Table
{
    /**
     * Returns an instance of this class.
     *
     * @return object FeedbackTable
     */
    public static function getInstance()
    {
        return Doctrine_Core::getTable('Feedback');
    }
    
    
    
    public function fetchall($params = array())
    {
        $q = Doctrine_Query::create()
            ->select('*')
            ->from('Feedback')
            ->orderBy('created_at DESC');
  
            // KEYWORD
            if(isset($params['keyword']) && $params['keyword'])
              $q->andWhere('name LIKE ? phone LIKE ? OR email LIKE ? OR body LIKE ?', array('%'.$params['keyword'].'%', '%'.$params['keyword'].'%', '%'.$params['keyword'].'%', '%'.$params['keyword'].'%'));
  
            // LIMIT
            $limit = isset($params['limit']) ? $params['limit'] : 100;
            $q->limit($limit);
    
        return $q->execute();
    }
  
  
    
    public function getPager($params = array(), $page=1)
    {
        $q = Doctrine_Query::create()
            ->select('*')
            ->from('Feedback')
            ->orderBy('created_at DESC');
  
            // KEYWORD
            if(isset($params['keyword']) && $params['keyword'])
              $q->andWhere('name LIKE ? phone LIKE ? OR email LIKE ? OR body LIKE ?', array('%'.$params['keyword'].'%', '%'.$params['keyword'].'%', '%'.$params['keyword'].'%', '%'.$params['keyword'].'%'));
  
            // LIMIT
            $limit = isset($params['limit']) ? $params['limit'] : 100;
            $q->limit($limit);
                
        $pager = new sfDoctrinePager('Feedback', sfConfig::get('app_pager', 30));
        $pager->setPage($page);
        $pager->setQuery($q);
        $pager->init();
        
        return $pager;
    }
}